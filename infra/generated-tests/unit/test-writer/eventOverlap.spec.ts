/**
 * @intent 이벤트 중첩 판단 로직의 경계 조건을 검증한다
 * @risk-level high
 */

describe('eventOverlap', () => {
  it('시작 시간이 동일한 이벤트를 중첩으로 처리한다', () => {
    throw new Error('Not implemented')
  })

  it('끝나는 순간과 시작 순간이 맞닿으면 중첩으로 보지 않는다', () => {
    throw new Error('Not implemented')
  })

  it('하루 이상 지속되는 이벤트는 모든 날짜에서 충돌을 감지한다', () => {
    throw new Error('Not implemented')
  })

  it('시간대 정보가 누락된 경우 예외를 던진다', () => {
    throw new Error('Not implemented')
  })
})

